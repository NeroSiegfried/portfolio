// No JavaScript. The expand/collapse is flex-grow + :hover, and the mobile
// sequence is a media query — which is the point: nothing here needs a
// resize listener, a state machine, or a re-render.
